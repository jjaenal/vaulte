# Test Environment Variables
NODE_ENV=test
PORT=3002

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production
JWT_EXPIRES_IN=1h

# API Configuration
API_KEY=test-api-key-for-testing

# Database (Mock for now)
DATABASE_URL=postgresql://test:test@localhost:5432/vaulte_test

# Redis (Mock for now)
REDIS_URL=redis://localhost:6379/1

# Email (Mock for now)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test
SMTP_PASS=test
FROM_EMAIL=test@vaulte.io

# OAuth (Mock for now)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
APPLE_CLIENT_ID=test-apple-client-id
APPLE_CLIENT_SECRET=test-apple-client-secret