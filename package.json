{
  "name": "vaulte-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Root tools for Vaulté monorepo (husky, lint-staged, prettier)",
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "prettier-plugin-solidity": "^1.3.1"
  },
  "lint-staged": {
    "*.{js,ts,tsx,json,md,css}": "prettier --write",
    "*.{sol}": "prettier --write"
  }
}
